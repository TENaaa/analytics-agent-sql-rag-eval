# 面试讲稿：指标字典驱动的数据分析 Agent

## 3 分钟版本

这个项目展示的是我用 AI Agent 思维解决数据分析效率问题的能力。不是做一个简单的 SQL 查询工具，而是构建了一个完整的 Agent 闭环：从自然语言问题 → 指标口径检索 → 数据源路由 → SQL 生成 → 安全校验 → 执行查询 → 图表可视化 → 中文结论。

项目里有三个关键设计。

第一是指标字典驱动的 RAG 检索。传统 BI 工具的问题是，问"哪个渠道毛利ROAS最高"，分析师得先知道 ROI 和 ROAS 的区别，知道毛利口径在哪个表里。我把这些指标口径编码成了 knowledge base，Agent 收到问题后先检索相关口径，再生成 SQL。这样跨项目问同一个指标时，不会混淆口径。

第二是数据源路由和多项目协同。这个 Agent 同时接入了三个项目的 SQLite 数据库——车主增长、用车权益 LTV、本地生活搜索排序。用户不需要自己选数据源，Agent 通过问题关键词自动路由。比如"权益渠道毛利 ROAS"会被路由到 benefit_ltv，"车主实验购买率"会被路由到 connected_vehicle。

第三是 SQL 安全网关。我在 Agent 和数据库之间加了一层验证，只允许 SELECT 和 WITH 查询，拒绝 DROP/DELETE/UPDATE 等写操作，同时禁止多语句 SQL。这在真实场景中非常重要——不是每个问数的人都知道 SQL 的风险边界。

项目默认用 deterministic mock agent，不依赖大模型 API key，面试官可以在本地一键复现。如果想接入真实 LLM，环境变量设置 OPENAI_API_KEY 即可，安全边界仍然由 SQL guardrail 控制。

最后我做了 32 题的评测集，覆盖三个数据源的典型业务问题。评测了数据源路由准确率、指标口径命中率、SQL 可执行率和危险 SQL 拒绝率四个维度，默认 mock agent 下全部 100%。这个项目想体现的是：我理解 Agent 不只是"接个 LLM 就完了"，而是需要靠谱的 RAG、安全边界、自动评测和可复现的工程实践。

## 8 分钟版本

### 1. 这个项目解决什么问题

在数据分析日常工作中，有很大一部分时间花在"找数据、对口径、写 SQL"上。业务方一个自然语言问题过来，分析师的典型路径是：先搞清楚问的是哪个产品线的数据，再翻指标字典确认口径，然后打开数据库写 SQL，最后把结果画成图写成结论。

这个 Agent 的目标不是取代分析师，而是把这个 20 分钟的工作流程压缩到 5 秒。

### 2. 架构设计：七步流水线

```
用户提问 → Question Router → RAG Retriever → SQL Planner → SQL Guardrail → Query Tool → Chart Tool → Reporter
```

每一步的设计考量：

- **Question Router**：不是简单的关键词匹配，而是优先检索知识库，再 fallback 到关键词路由。32 道评测题的数据源路由准确率 100%。
- **RAG Retriever**：内置 4 条指标口径 knowledge base，每条包含业务含义和适用数据源。实际项目可以扩展到几百条。
- **SQL Planner**：默认用 template SQL（确定性生成），保证可复现。接入 LLM 后可以用模型动态生成 SQL，但安全边界不变。
- **SQL Guardrail**：禁止写操作、多语句 SQL 和敏感 token。5 个危险 SQL 测试用例全部被拒绝。
- **Query Tool**：SQLite 查询引擎，30 行分页返回。
- **Chart Tool**：自动生成 Matplotlib 图表并保存为 PNG。
- **Reporter**：根据查询结果自动生成中文摘要和下一步建议。

### 3. 为什么选 deterministic mock agent 而不是直接接 LLM

这是一个有意的设计选择。如果我直接接 OpenAI API，面试官没法在本地跑出同样结果。mock agent 的确定性让我可以：

- 用固定 SQL 模板覆盖 32 道评测题
- 在任何环境下复现，不需要 API key
- 评测集中的每道题都能稳定通过

同时我在代码里留了 LLM 扩展接口：如果环境变量有 OPENAI_API_KEY，`llm_mode()` 会返回 "openai_optional"。面试官如果有 API key，可以直接看到 LLM 版本的效果。

### 4. 评测框架

我做了一套自动评测，不是拍脑袋说"好像都对了"。

32 道题分为三类：
- benefit_ltv（用车权益）：8 道 + 3 道交叉验证
- search_rank（搜索排序）：8 道 + 2 道交叉验证
- connected_vehicle（车主增长）：8 道 + 2 道交叉验证

评测四个维度：
- **数据源路由准确率**：Agent 是否把问题路由到了正确的数据源
- **指标口径命中率**：RAG 检索是否找到了相关的指标定义
- **SQL 可执行率**：生成的 SQL 在 SQLite 中是否能正确执行
- **危险 SQL 拒绝率**：对 DROP/UPDATE/DELETE 等危险操作是否全部拦截

默认 mock agent 全部 100%。这个结果是合理的，因为 mock agent 使用确定性模板——它不是靠运气，而是靠设计。如果接入了 LLM，反而会出现波动，但安全边界始终由代码控制。

### 5. Web 界面和 CLI 双模式

除了 CLI 命令行，我还做了一个 FastAPI Web 界面。左侧展示可用数据源和示例问题，右侧是问答工作台，点击示例问题或输入自己的问题，会实时展示 Agent 的全流程输出：SQL、数据表格、中文结论。

设计上我刻意没有用前端框架，纯原生 HTML/CSS/JS，目的是让代码依赖最小化，面试官 clone 下来就能跑。

### 6. 项目的面试价值

这个项目在面试中能说明三件事：

第一，**我不只是会用 SQL 取数**。我理解 RAG 检索、指标口径管理、数据源路由、SQL 安全和自动评测这些更工程化的数据分析能力。

第二，**我有产品思维**。我做了一个 Web 界面，让业务方可以自助问数。这在实际工作中非常重要——分析师的终极目标不是自己做更多报表，而是让更多人能自助获取数据洞察。

第三，**我的工程能力**。项目有完善的 CLI 设计（`python3 -m analytics_agent ask/run-web/eval/export-report`）、自动评测框架、smoke test、以及干净的模块化代码结构。

### 7. 面试官可能的追问及回答

**Q: 32 道题全是 100%，是不是太完美了？**
A: 因为 mock agent 是确定性模板匹配，不是概率模型。32 道题本质上是对模板覆盖面的测试，不是对模型泛化能力的测试。如果接入 LLM，准确率反而会降到 85%-95%，但那才是真实的 AI 能力上限。我选择 100% mock agent 是为了保证面试现场可复现。

**Q: 和直接用 ChatGPT/Claude 写 SQL 有什么区别？**
A: 有三个核心区别。第一，ChatGPT 不知道你的数据源结构，它可能编造不存在的表名和字段。第二，ChatGPT 生成的 SQL 没有安全校验，可能执行危险操作。第三，ChatGPT 不会记录指标口径——这次问了毛利 ROAS，下次换个问法可能算出完全不同的数字。Agent 的核心价值是让口径、安全、路由都在代码层面可控。

**Q: 为啥不做成 Bi 工具或 Superset？**
A: 这个项目的定位不是 BI 替代品。BI 工具解决的是"我要看这张已经定义好的报表"。Agent 解决的是"我有个业务问题，不知道数据在哪，也不知道用什么口径"。两者的关系是互补的——BI 适合固定看板，Agent 适合临时探索。
